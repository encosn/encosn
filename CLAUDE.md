# hub — 중학교 정보 수업 활동 메인페이지

> 한 줄 소개: **단원 1~5 를 고르면 그 단원의 활동과 성취기준이 나오는 입구 페이지**

## ① 앱의 목적과 대상 단원

- **목적**: 흩어져 있는 수업용 웹앱들을 한 주소로 모아, 수업 중 주소를 하나만 알려 주면
  학생이 단원 → 활동 순서로 찾아 들어갈 수 있게 한다.
- **대상**: 중학교 1~3학년 학생과 교사
- **대상 단원**: 특정 단원이 아니라 **정보 과목 5개 영역 전체의 입구**다.
  (1 컴퓨팅 시스템 / 2 데이터 / 3 알고리즘과 프로그래밍 / 4 인공지능 / 5 디지털 문화)
- **성취기준**: 이 페이지 자체는 성취기준에 연결되지 않는다. 각 **활동**이 연결된다.
- **배포 주소**: **https://encosn.github.io/encosn/** (저장소 `encosn/encosn`)

### 왜 저장소 이름이 `hub` 가 아니라 `encosn` 인가

`encosn/encosn` 저장소가 이미 있었고(엉뚱하게 excel-picker 빌드본이 올라가 있었다),
사용자가 새 저장소를 만드는 대신 그것을 재사용하기로 정했다 (2026-08-02).
그래서 **로컬 폴더 이름은 `hub`, GitHub 저장소 이름은 `encosn`** 으로 서로 다르다.

주소를 `https://encosn.github.io/` (슬래시 하나)로 짧게 만들고 싶어지면
`encosn.github.io` 라는 이름의 저장소를 새로 만들어 이 내용을 옮기면 된다.
그때 각 활동 앱의 README 와 이 문서의 주소도 함께 고쳐야 한다.

## 파일 구성

| 파일 | 설명 | 종류 |
|:---|:---|:---|
| `index.html` | 메인. 단원 카드 5개 + 선생님용 도구 | 소스 |
| `unit1.html` ~ `unit5.html` | 단원 페이지. **5개 파일이 `<body data-unit="N">` 한 글자만 다르다** | 소스 |
| `data.js` | **단원·활동 데이터.** 활동을 추가할 때 고치는 유일한 파일 | 소스 |
| `app.js` | `data.js` 를 읽어 메인/단원 페이지를 그린다 | 소스 |
| `style.css` | 공통 스타일. 교실 TV 기준 기본 20px | 소스 |
| `README.md` | GitHub 저장소 첫 화면에 나오는 소개 | 소스 |
| `package.json` / `vite.config.mjs` | 개발 서버용 (포트 8084) | 소스 |
| `node_modules/` | 설치된 패키지 (vite 등) | 생성물 |

## ② 기능 목록

### 메인페이지 (`index.html`)

- **단원 카드 5개** — 단원 번호 배지, 이모지, 단원명, 한 줄 요약,
  하단에 `활동 N개`(또는 `활동 준비 중`) · `성취기준 M개` 배지. 카드 전체가 링크다.
- 단원마다 왼쪽 테두리 색이 다르다 (`data.js` 의 `color`).
- **선생님용 도구** 섹션 — 성취기준에 연결되지 않은 도구는 단원 카드와 섞지 않고 아래에 따로 둔다.

### 단원 페이지 (`unit1.html` ~ `unit5.html`)

- `← 단원 고르기로` 되돌아가기 링크
- 단원 제목 + 요약 (그 단원 색이 위 테두리에 들어간다)
- **해 볼 수 있는 활동** — 활동 카드. `status: 'ready'` 면 큰 「활동 열기 →」 버튼,
  `'soon'` 이면 흐린 「준비 중」 딱지. 활동이 하나도 없으면 🛠️ 안내 카드.
- **이 단원의 성취기준** — 그 단원 성취기준 전체를 코드와 함께 보여 준다.

### 활동을 추가하는 방법 (제일 자주 할 일)

`data.js` 의 해당 단원 `activities` 배열에 항목 하나를 넣으면 끝이다.
메인페이지의 「활동 N개」 숫자도 이 배열을 세어서 자동으로 바뀐다.

```js
{
  title: '활동 이름',
  emoji: '🔢',
  desc: '한두 문장 설명',
  standards: ['9정02-03'],                       // 코드만. 문구는 단원 standards 에 이미 있다
  url: 'https://encosn.github.io/새-활동/',      // 다른 저장소에 배포된 앱
  status: 'ready',                               // 만드는 중이면 'soon' (url 없어도 됨)
}
```

## 설계에서 알아 둘 것 (헷갈리기 쉬운 부분)

- **주소는 전체 주소(`https://…`)로 적는다.** 활동 앱들이 **각각 다른 저장소**에 배포되어 있어서
  상대 경로로는 갈 수 없다. 전체 주소로 적으면 로컬에서 더블클릭해 열어도 링크가 동작한다.
- **스크립트를 ES 모듈로 바꾸지 말 것.** 보통 `<script src>` 라서 서버 없이 더블클릭으로 열린다.
  `type="module"` 로 바꾸면 `file://` 에서 CORS 로 막힌다. (spread-simulator 와 같은 사정)
- **빌드 단계가 없다.** `package.json` 에 `build` 스크립트를 일부러 넣지 않았다.
  GitHub Pages 가 저장소 루트의 소스를 그대로 서비스하므로 **배포 = `git push` 하나**다.
  (`.github/workflows/` 도 없다. 다른 앱 3개와 다른 점이다)
- **카드 바닥 `[활동 N개] [성취기준 M개] [들어가기 →]` 는 반드시 한 줄이어야 한다** (2026-08-03 요청).
  폭이 빠듯해서 아래 값들이 서로 묶여 있다. **하나만 바꿔도 줄이 접힌다.**

  | 곳 | 값 | 이유 |
  |:---|:---|:---|
  | `.wrap` max-width | 1180px | 카드 360px × 3열 + 여백이 들어가는 최소 폭 |
  | `.unit-grid` minmax | 360px (≤1100px 에선 340px) | 카드 안쪽 299px 확보 (필요 278px) |
  | `.unit-card` padding | 좌우 22px | 26px 이면 21px 모자란다 |
  | `.badge` | 14px / 좌우 11px (≤1100px 에선 13px / 10px) | |
  | `.go` | 15px (≤1100px 에선 14px) | |
  | 배지 글자 | **「활동 준비 중」이 아니라 「준비 중」** | 32px 차이. 이것 때문에 접혔다 |

  - `.badge`·`.go` 에 `flex: 0 0 auto` + `white-space: nowrap` 을 준다. `flex-wrap: nowrap` 만으로는
    부족하다 — 칸이 좁아지면 flex 항목이 줄어들며 **배지 안에서** 글자가 접힌다.
  - `.badge.shrink`(성취기준 배지)만 줄어들며 말줄임(…) 된다. 마지막 안전장치이고 320px 폰에서만 작동한다.
  - `.unit-card` 의 `min-width: 0` 을 지우면 320px 폰에서 카드가 화면 밖으로 나간다
    (그리드 항목 기본값이 `min-width: auto` 라서).
  - 검증: 320·360·390·414·500·640·700·768·800·900·1000·1120·1180·1400·1920px 에서
    전부 한 줄·가로 스크롤 없음 확인 (2026-08-03).
- **`vite.config.mjs`** — `package.json` 에 `"type": "module"` 이 없어서 확장자가 `.mjs` 여야 한다.
- `color-mix()` 를 쓰는 곳마다 **바로 위 줄에 대체값**을 두었다. 교실의 오래된 브라우저에서
  색이 통째로 빠지지 않게 하기 위한 것이니, 고칠 때 두 줄을 함께 고칠 것.
- 성취기준 문구는 `../01.교육과정/중학교 교육과정_성취기준.md` 에서 옮겼다.
  원문이 바뀌면 `data.js` 도 함께 고쳐야 한다 (자동으로 따라가지 않는다).

## 실행 방법

**① 그냥 더블클릭** — `index.html`. 서버·인터넷 없이 열린다.
(다만 Jua 글꼴은 Google Fonts 에서 받으므로 오프라인이면 맑은 고딕으로 나온다)

**② 개발 서버** — 이 폴더에서
```
npm install     (처음 한 번)
npm start       → http://localhost:8084
```
루트에서 실행할 때는 `npm --prefix ./hub start`.

**③ 배포** — 이 폴더에서 `git push origin main`. 1분쯤 뒤 반영된다.

## ③ 현재 개발 상태와 다음 할 일

### 상태 (2026-08-02, 1차 완성)

- [x] 메인페이지 (단원 카드 5개 + 선생님용 도구)
- [x] 단원 페이지 5개 (활동 목록 + 성취기준 + 활동 없을 때 안내)
- [x] `data.js` 한 파일로 두 화면을 함께 관리
- [x] 좁은 화면·인쇄 대응
- [x] `encosn/encosn` 에 push 완료 → https://encosn.github.io/encosn/
- [x] 로컬 확인: 메인·2단원(활동 있음)·1단원(활동 없음) 렌더링과 콘솔 무오류 확인

### 링크 대상 앱의 배포 상태

| 단원 | 활동 | 주소 | 상태 |
|:---:|:---|:---|:---|
| 2 | 10진수 → 2진수 변환 실습기 | https://encosn.github.io/binary-converter/ | 저장소 생성 대기 |
| 2 | 공공데이터 결측치 정리 실습기 | https://encosn.github.io/data-cleaner/ | 저장소 생성 대기 |
| 5 | 정보의 확산 속도 체험하기 | https://encosn.github.io/digit/ | 살아 있음 |
| — | 엑셀 다듬기 (선생님용) | https://encosn.github.io/excel-picker/ | 살아 있음 |

### 다음 할 일

- [ ] `encosn/binary-converter`, `encosn/data-cleaner` 저장소를 만들고 push
      → 2단원 링크 2개가 그때 살아난다 (지금은 404)
- [ ] 1·3·4단원 활동 만들기 (컴퓨팅 시스템 / 알고리즘과 프로그래밍 / 인공지능)
- [ ] 화면 디자인을 실제 교실 TV에서 눈으로 확인 (글자 크기·색 대비)
- [ ] 활동이 늘어나면 단원 페이지에 차시·난이도 표시를 넣을지 검토
